<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Sorry Afra</title>

<!-- Fonts -->
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@500;700&family=Poppins:wght@300;400&family=Great+Vibes&display=swap" rel="stylesheet">

<style>
*{margin:0;padding:0;box-sizing:border-box;}

body{
  font-family:'Poppins',sans-serif;
  height:100vh;
  background:url('bg.png') no-repeat center/cover;
  overflow:hidden;
}

/* Overlay */
.overlay{
  position:fixed;
  width:100%;
  height:100%;
  background:rgba(0,0,0,0.2);
  backdrop-filter: blur(3px);
}

/* Start screen */
.start{
  position:fixed;
  width:100%;
  height:100%;
  background:rgba(0,0,0,0.8);
  display:flex;
  justify-content:center;
  align-items:center;
  flex-direction:column;
  color:white;
  z-index:10;
}

/* Pages */
.page{
  position:absolute;
  width:100%;
  height:100%;
  display:flex;
  justify-content:center;
  align-items:center;
  flex-direction:column;
  color:white;
  opacity:0;
  transition:1s;
  pointer-events:none;
}

.active{
  opacity:1;
  pointer-events:all;
}

/* Card */
.card{
  background:rgba(255,255,255,0.15);
  backdrop-filter:blur(20px);
  padding:25px;
  border-radius:20px;
  width:90%;
  max-width:420px;
  box-shadow:0 10px 40px rgba(0,0,0,0.4);
}

/* Fonts */
.card h1,.card h2{
  font-family:'Playfair Display',serif;
}

.afra{
  font-family:'Great Vibes',cursive;
  font-size:1.3em;
  color:#ffd1dc;
}

/* Button */
button{
  margin-top:15px;
  padding:10px 20px;
  border:none;
  border-radius:20px;
  background:linear-gradient(45deg,#ff8fab,#ff4d6d);
  color:white;
  cursor:pointer;
}

/* Petals */
.petal{
  position:fixed;
  top:-10px;
  width:10px;
  height:10px;
  background:radial-gradient(circle,#ff8fab,#ff4d6d);
  border-radius:50%;
  animation:fall linear infinite;
}

@keyframes fall{
  to{transform:translateY(110vh) rotate(360deg);}
}
</style>
</head>

<body>

<div class="overlay"></div>

<!-- Start screen -->
<div class="start" id="startScreen">
  <h2>Play the music and continue</h2>
  <button onclick="startSite()">Start</button>
</div>

<!-- Music -->
<audio id="music" loop>
  <source src="music.mp3" type="audio/mpeg">
</audio>

<!-- Page 1 -->
<div class="page active" id="p1">
  <div class="card">
    <h2 id="t1"></h2>
    <p id="t2"></p>
    <button onclick="next(2)">Next</button>
  </div>
</div>

<!-- Page 2 -->
<div class="page" id="p2">
  <div class="card">
    <h2 id="t3"></h2>
    <p id="t4"></p>
    <button onclick="next(3)">Next</button>
  </div>
</div>

<!-- Page 3 -->
<div class="page" id="p3">
  <div class="card">
    <h2 id="t5"></h2>
    <p id="t6"></p>
    <button onclick="next(4)">Next</button>
  </div>
</div>

<!-- Page 4 -->
<div class="page" id="p4">
  <div class="card">
    <h2 id="t7"></h2>
    <p id="t8"></p>
    <button onclick="next(5)">Next</button>
  </div>
</div>

<!-- Final -->
<div class="page" id="p5">
  <div class="card">
    <h1 id="t9"></h1>
  </div>
</div>

<script>

/* Start (music fix) */
function startSite(){
  const music = document.getElementById("music");
  music.play().then(()=>{
    document.getElementById("startScreen").style.display="none";
  }).catch(()=>{
    alert("Click again to enable music");
  });
}

/* Page switch */
function next(n){
  document.querySelectorAll('.page').forEach(p=>p.classList.remove('active'));
  document.getElementById('p'+n).classList.add('active');
}

/* Typing (HTML-safe FIXED) */
function type(id,text,delay=40){
  let i=0;
  function t(){
    if(i<=text.length){
      document.getElementById(id).innerHTML = text.substring(0,i);
      i++;
      setTimeout(t,delay);
    }
  }
  t();
}

/* Text */
type("t1","Assalamu Alaikum <span class='afra'>Afra</span>...");
setTimeout(()=>type("t2","I am really sorry for everything."),1000);

setTimeout(()=>type("t3","I truly mean it."),2000);
setTimeout(()=>type("t4","It will not happen again. I will be more careful."),3000);

setTimeout(()=>type("t5","A small story..."),4000);
setTimeout(()=>type("t6","Sometimes people come into our lives unexpectedly, and slowly they become important without us even realizing it."),5000);

setTimeout(()=>type("t7","Our Journey"),6000);
setTimeout(()=>type("t8","I never expected we would become such good friends, but I am really glad that we did."),7000);

setTimeout(()=>type("t9","I am really sorry, <span class='afra'>Afra</span>."),8000);

/* Petals */
function petal(){
  let p=document.createElement("div");
  p.className="petal";
  p.style.left=Math.random()*100+"vw";
  p.style.animationDuration=(4+Math.random()*5)+"s";
  document.body.appendChild(p);
  setTimeout(()=>p.remove(),9000);
}
setInterval(petal,250);

</script>

</body>
</html>